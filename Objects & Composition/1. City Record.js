function record(name, pop, treasury){
    const cityRecord = {
        'name' : name,
        'population' : pop,
        'treasury' : treasury
    }
    return(cityRecord);
}

console.log(record('Tortuga',

7000,

15000));